@model MockInterviews.Models.ViewModels.ExternalUserProfileViewModel
@{
    ViewData["Title"] = Model.FirstName + " " + Model.LastName + "'s Profile";
    //ViewData["ActivePage"] = ManageNavPages.Index;
}

<h3>@Model.FirstName @Model.LastName</h3>

<div class="row">
    <div class="col-md-6">
        @*<label>Phone number:</label>
        @if(Model.Input.PhoneNumber != null)
        {
            <span>@Model.Input.PhoneNumber</span>
        }
        else
        {
            <span>No phone number specified</span>
        }
        <div>
        </div>*@

            <div>
                <label>Class:</label>
                <span>@Model.Class</span>
            </div>
            <div style="padding-top: 15px;">
                <label>Profile Picture:</label>
                <br />
                <img src="~/images/SabanHeadshot.jpg"
                     alt="Default profile picture"
                     style="width: 350px; height: 350px; object-fit: cover" />
            </div>
    </div>
    <a asp-controller="Home" asp-action="Index">Back to Home</a>

</div>
