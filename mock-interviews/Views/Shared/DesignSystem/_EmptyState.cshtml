@model UiEmptyStateViewModel

<div class="px-6 py-10 text-center">
    <ui-icon name="@Model.Icon" class="mx-auto size-10 text-muted" />
    <h2 class="mt-4 text-lg font-semibold text-ink">@Model.Title</h2>
    <p class="mx-auto mt-1 max-w-sm text-sm leading-6 text-muted">@Model.Description</p>
</div>
